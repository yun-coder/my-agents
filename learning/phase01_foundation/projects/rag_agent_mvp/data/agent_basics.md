# Agent 基础

Agent 不只是一次模型调用。一个基础 Agent 通常包含模型、提示词、工具、记忆和控制流程。

# RAG

RAG 是 Retrieval-Augmented Generation 的缩写。它会先从外部知识库检索相关资料，再把资料作为上下文交给模型生成答案。

# 工具调用

工具调用时，模型负责提出结构化调用请求。真正执行数据库查询、网络请求或文件操作的是应用程序。
